{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "EXPO_ENV=production npx expo export --platform web --output-dir dist",
  "outputDirectory": "dist",
  "installCommand": "npm ci",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
